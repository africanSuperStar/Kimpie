pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "KMM_App_Builder"
include(":android")
include(":shared")
include(":parsec")
